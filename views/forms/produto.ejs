<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script src="https://cdn.tailwindcss.com"></script>
    <title>Cadastrar Produto - Loja Vilalva</title>
</head>

<body class="flex justify-center items-center min-h-screen bg-gray-100">
    <div class="w-full max-w-4xl bg-white shadow-lg rounded-lg overflow-hidden">
        <div class="p-6">
            <h2 class="text-3xl font-semibold text-gray-800 mb-6 text-center">Cadastrar Produto</h2>

            <form action="/" method="POST" class="space-y-4">
                <div>
                    <label for="nome" class="block text-gray-700 font-semibold mb-2">Nome do Produto</label>
                    <input 
                        type="text" 
                        id="nome" 
                        name="nome" 
                        class="w-full px-4 py-2 border rounded-lg focus:outline-none focus:ring-2 focus:ring-indigo-500" 
                        placeholder="Digite o nome do produto" 
                        required>
                </div>

                <div>
                    <label for="descricao" class="block text-gray-700 font-semibold mb-2">Descrição</label>
                    <textarea 
                        id="descricao" 
                        name="descricao" 
                        class="w-full px-4 py-2 border rounded-lg focus:outline-none focus:ring-2 focus:ring-indigo-500" 
                        placeholder="Digite a descrição do produto" 
                        rows="4" 
                        required></textarea>
                </div>

                <div>
                    <label for="preco" class="block text-gray-700 font-semibold mb-2">Preço (R$)</label>
                    <input 
                        type="number" 
                        id="preco" 
                        name="preco" 
                        step="0.01" 
                        class="w-full px-4 py-2 border rounded-lg focus:outline-none focus:ring-2 focus:ring-indigo-500" 
                        placeholder="Digite o preço do produto" 
                        required>
                </div>

                <div class="flex flex-col mb-4">
                    <label for="imagem" class="text-sm font-semibold">URL da Imagem do Produto</label>
                    <input type="text" id="imagem" name="imagem" placeholder="Insira a URL da imagem"
                        class="border-solid border-2 border-indigo-600 p-2 outline-none rounded">
                </div>

                <div>
                    <label for="quantidadeEstoque" class="block text-gray-700 font-semibold mb-2">Quantidade no Estoque</label>
                    <input 
                        type="number" 
                        id="quantidadeEstoque" 
                        name="quantidadeEstoque" 
                        class="w-full px-4 py-2 border rounded-lg focus:outline-none focus:ring-2 focus:ring-indigo-500" 
                        placeholder="Quantidade disponível no estoque" 
                        required>
                </div>
                

                <div class="flex justify-center">
                    <button 
                        type="submit" 
                        class="bg-indigo-600 text-white font-bold py-2 px-6 rounded hover:bg-indigo-700 transition duration-300">
                        Cadastrar Produto
                    </button>
                </div>
            </form>

            <div class="mt-6 text-center">
                <a href="/" class="text-indigo-500 hover:underline">Voltar para a loja</a>
            </div>
        </div>
    </div>
</body>

</html>
